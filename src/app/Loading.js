import React from 'react';

const Loading = () => {
    return (
        <div className='flex items-center p-100 justify-center '>
            <span className="loading loading-spinner  text-success"></span>
        </div>
    );
};

export default Loading;